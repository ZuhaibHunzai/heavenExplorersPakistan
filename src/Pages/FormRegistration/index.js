import FormRegistation from "../../Components/FormRegistration";

const FormRegPage = () => {
  window.scrollTo({ left: 0, top: 0, behavior: "smooth" });
  return <FormRegistation />;
};
export default FormRegPage;
