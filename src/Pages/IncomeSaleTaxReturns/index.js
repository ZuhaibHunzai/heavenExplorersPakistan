import IncomeSalesTaxReturns from "../../Components/IncomeSalesTaxReturns";

const IncomeSalesPage = () => {
  window.scrollTo({ left: 0, top: 0, behavior: "smooth" });
  return <IncomeSalesTaxReturns />;
};
export default IncomeSalesPage;
